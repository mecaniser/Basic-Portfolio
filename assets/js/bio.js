$(document).ready(function () {
    $('.tooltipped').tooltip({
        position: "top"
    });
    $('.dropdown-trigger').dropdown();
    $('.fixed-action-btn').floatingActionButton({
        direction:"left",
    });
});