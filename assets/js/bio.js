$(document).ready(function () {
    $('.tooltipped').tooltip({
        position: "top"
    });
    $('.dropdown-trigger').dropdown({
        alignment: "left"
    });
    $('.fixed-action-btn').floatingActionButton({
        direction:"left",
    });
});